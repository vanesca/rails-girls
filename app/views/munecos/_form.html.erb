<%= form_for(@muneco) do |f| %>
  <% if @muneco.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@muneco.errors.count, "error") %> prohibited this muneco from being saved:</h2>

      <ul>
      <% @muneco.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    Nombre<br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    Descripcion<br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    Costo<br>
    <%= f.text_field :cost %>
  </div>
  <div class="field">
    Foto<br>
    <%= f.file_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
